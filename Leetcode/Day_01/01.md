## 1️⃣ LeetCode Singly Linked List Node (JS)

LeetCode already gives this, but good to remember:

```js
function ListNode(val, next) {
    this.val = val === undefined ? 0 : val;
    this.next = next === undefined ? null : next;
}
```

---

## 2️⃣ Traverse a Linked List

```js
var traverse = function(head) {
    let curr = head;
    while (curr) {
        console.log(curr.val);
        curr = curr.next;
    }
};
```

---

## 3️⃣ Reverse a Linked List (MOST IMPORTANT 🔥)

```js
var reverseList = function(head) {
    let prev = null;
    let curr = head;

    while (curr) {
        let next = curr.next;
        curr.next = prev;
        prev = curr;
        curr = next;
    }
    return prev;
};
```

---

## 4️⃣ Fast & Slow Pointer Template

Used in many problems.

```js
let slow = head;
let fast = head;

while (fast && fast.next) {
    slow = slow.next;
    fast = fast.next.next;
}
```

---

## 5️⃣ Detect Cycle (Floyd’s Algorithm)

```js
var hasCycle = function(head) {
    let slow = head;
    let fast = head;

    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
        if (slow === fast) return true;
    }
    return false;
};
```

---

## 6️⃣ Find Middle of Linked List

```js
var middleNode = function(head) {
    let slow = head;
    let fast = head;

    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
    }
    return slow;
};
```

---

## 7️⃣ Remove Nth Node From End (Dummy Node Pattern)

```js
var removeNthFromEnd = function(head, n) {
    let dummy = new ListNode(0, head);
    let slow = dummy;
    let fast = dummy;

    for (let i = 0; i <= n; i++) {
        fast = fast.next;
    }

    while (fast) {
        slow = slow.next;
        fast = fast.next;
    }

    slow.next = slow.next.next;
    return dummy.next;
};
```

---

## 8️⃣ Merge Two Sorted Linked Lists

```js
var mergeTwoLists = function(l1, l2) {
    let dummy = new ListNode(0);
    let curr = dummy;

    while (l1 && l2) {
        if (l1.val < l2.val) {
            curr.next = l1;
            l1 = l1.next;
        } else {
            curr.next = l2;
            l2 = l2.next;
        }
        curr = curr.next;
    }

    curr.next = l1 || l2;
    return dummy.next;
};
```

---

## 9️⃣ Palindrome Linked List

```js
var isPalindrome = function(head) {
    let slow = head;
    let fast = head;

    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
    }

    // reverse second half
    let prev = null;
    while (slow) {
        let next = slow.next;
        slow.next = prev;
        prev = slow;
        slow = next;
    }

    // compare halves
    let left = head;
    let right = prev;

    while (right) {
        if (left.val !== right.val) return false;
        left = left.next;
        right = right.next;
    }
    return true;
};
```

---

## 🔟 Delete Node (Given Only Node)

```js
var deleteNode = function(node) {
    node.val = node.next.val;
    node.next = node.next.next;
};
```

---

## 🔥 Universal Dummy Node Template (MEMORIZE)

```js
let dummy = new ListNode(0);
dummy.next = head;
let curr = dummy;
```
